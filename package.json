{
  "name": "treatment-center-starter",
  "private": true,
  "version": "0.1.0",
  "description": "Starter scaffolding for treatment-center websites — static HTML assembled by a small Node build.",
  "scripts": {
    "build": "npm run build:css && npm run build:js && npm run build:html",
    "build:css": "csso css/styles.css -o css/styles.min.css",
    "build:js": "terser js/main.js --compress --mangle -o js/main.min.js",
    "build:html": "node scripts/build-html.mjs"
  },
  "devDependencies": {
    "csso-cli": "^4.0.2",
    "sharp": "^0.34.5",
    "terser": "^5.36.0"
  }
}
